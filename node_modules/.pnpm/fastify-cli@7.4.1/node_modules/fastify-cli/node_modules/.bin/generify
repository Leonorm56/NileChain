#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../generify/generify.js" "$@"
else
  exec node  "$basedir/../../../generify/generify.js" "$@"
fi
# cmd-shim-target=C:/Users/GH/Desktop/NILECHAIN/node_modules/.pnpm/fastify-cli@7.4.1/node_modules/generify/generify.js
